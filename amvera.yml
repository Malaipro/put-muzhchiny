meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile
  skip: false
  noCache: true
run:
  image: null
  command: null
  args: null
  persistenceMount: /data
  containerPort: "80"
